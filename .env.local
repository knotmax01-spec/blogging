# Frontend Configuration for Local Development

# API Base URL (proxied through Vite during dev)
VITE_API_URL=/api

# For production deployment to Fly.io, change to:
# VITE_API_URL=https://your-app.fly.dev/api
